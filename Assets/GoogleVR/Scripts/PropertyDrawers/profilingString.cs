using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class profilingString : MonoBehaviour
{
    // Start is called before the first frame update
    void Update()
    {
        for (int i=0;1<100f;i++) {
            List<string> test = new List<string>(100);
        }
    }

   
}
